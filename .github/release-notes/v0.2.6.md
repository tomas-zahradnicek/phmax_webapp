## PHmax webapp 0.2.6

### UX a dashboard
- Dashboard deep-link pro **ŠD** a **NV75** (stejný vzor jako PV/ZŠ/SŠ).
- Verdikt ŠD na dashboardu varuje u neúplných vstupů.

### Kvalita a refaktor
- E2E dashboard deep-link pro všech 5 modulů s chybami vstupů.
- ZŠ: `zs-page-handlers.ts` (export + audit + srovnání).
- ESLint **0** warnings v CI.

### Test plan
- [ ] Dashboard – Vyžaduje pozornost → ŠD/NV75 posune na sekci/řádek
- [ ] ZŠ – export CSV/XLSX a audit JSON po refaktoru handlerů
- [ ] `npm test`, `npm run lint`, `npm run test:e2e`, `npm run build`
